Estudo pessoal sobre a diferenca entre ENTRYPOINT e CMD commands no docker

```sh
docker build -t estudo:latest .
docker run --rm estudo:latest echo "oi"
```
```sh
docker rmi estudo:latest
```

```sh
docker compose up
```